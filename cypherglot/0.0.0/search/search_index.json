{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"CypherGlot","text":"<p>CypherGlot is the Cypher frontend compiler for the HumemAI stack.</p> <p>The intended flow is:</p> <pre><code>raw Cypher string\n\u2192 parse\n\u2192 Cypher AST\n\u2192 compile\n\u2192 SQLGlot AST\n</code></pre> <p>The repository is intentionally compiler-focused.</p> <ul> <li>It parses and lowers Neo4j-like Cypher.</li> <li>It returns SQLGlot AST instead of owning SQL execution.</li> <li>It can carry vector intent forward, but it does not execute vector search.</li> </ul>"},{"location":"#what-this-repo-is-for","title":"What this repo is for","text":"<ul> <li>a reusable Cypher frontend compiler</li> <li>a stable boundary between Cypher parsing and execution</li> <li>a SQLGlot-based output that host runtimes can plan and execute</li> </ul>"},{"location":"#what-this-repo-is-not","title":"What this repo is not","text":"<ul> <li>a database engine</li> <li>a SQL executor</li> <li>a vector search engine</li> <li>a storage system</li> </ul>"},{"location":"#start-here","title":"Start here","text":"<ul> <li>Installation</li> <li>Quick Start</li> <li>Compiler Contract</li> <li>Roadmap</li> </ul>"},{"location":"development/documentation/","title":"Documentation","text":"<p>CypherGlot uses MkDocs Material for its documentation site.</p> <p>Build the docs locally with:</p> <pre><code>uv sync --group docs\nuv run mkdocs build --strict\n</code></pre> <p>The versioned docs deployment path mirrors the HumemAI docs workflow used by the other repositories in this workspace.</p>"},{"location":"development/release/","title":"Release Workflow","text":"<p>CypherGlot's public release path centers on two outputs:</p> <ul> <li>the PyPI package</li> <li>the versioned docs site</li> </ul>"},{"location":"development/release/#package-release","title":"Package release","text":"<p>Package versions come from Git tags that match the Hatch VCS pattern:</p> <pre><code>v0.1.0\nv0.1.1\nv0.2.0\n</code></pre> <p>The PyPI workflow builds the package, checks metadata, verifies that the wheel installs, and publishes through PyPI trusted publishing.</p>"},{"location":"development/release/#docs-release","title":"Docs release","text":"<p>The docs deployment workflow:</p> <ul> <li>checks out <code>cypherglot</code></li> <li>installs the docs dependencies</li> <li>checks out <code>humemai-docs</code></li> <li>runs <code>mike deploy</code> with the <code>cypherglot</code> prefix</li> <li>optionally updates the <code>latest</code> alias</li> </ul>"},{"location":"development/release/#shared-docs-hub","title":"Shared docs hub","text":"<p>The shared docs landing page at <code>https://docs.humem.ai/</code> can publish <code>cypherglot</code> as its own section, separate from <code>humemdb</code>.</p>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>Install <code>cypherglot</code> from PyPI:</p> <pre><code>pip install cypherglot\n</code></pre> <p>For local development with docs tooling:</p> <pre><code>uv sync --group docs\n</code></pre>"},{"location":"getting-started/installation/#python-support","title":"Python support","text":"<p><code>cypherglot</code> targets Python <code>3.10+</code>.</p>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":"<p>CypherGlot is being set up as a reusable Cypher frontend compiler. The package currently exposes versioned package metadata while the parser and lowering API are being filled in.</p> <p>Today, the main practical quick start is to install the package and track the compiler contract and roadmap while the parser surface lands.</p> <p>The intended long-term shape is a small API that accepts raw Cypher and returns a SQLGlot AST that a host runtime can execute.</p>"},{"location":"guide/compiler-contract/","title":"Compiler Contract","text":"<p>The intended contract for CypherGlot is:</p> <pre><code>raw Cypher string\n\u2192 parse\n\u2192 Cypher AST\n\u2192 compile\n\u2192 SQLGlot AST\n</code></pre> <p>That means:</p> <ul> <li><code>cypherglot</code> owns raw Cypher parsing</li> <li><code>cypherglot</code> owns Cypher normalization and lowering</li> <li><code>cypherglot</code> returns SQLGlot <code>Expression</code> trees</li> <li>a host runtime such as <code>humemdb</code> owns planning, vector execution, dialect   generation, and backend execution</li> </ul>"},{"location":"guide/compiler-contract/#scope","title":"Scope","text":"<p><code>cypherglot</code> should:</p> <ul> <li>parse Neo4j-like Cypher input</li> <li>validate admitted subset boundaries clearly</li> <li>lower admitted Cypher into SQLGlot AST</li> </ul> <p><code>cypherglot</code> should not:</p> <ul> <li>execute SQL</li> <li>own graph storage</li> <li>execute vector search</li> <li>manage vector index lifecycle</li> </ul>"},{"location":"guide/compiler-contract/#vector-aware-but-not-vector-executing","title":"Vector-aware but not vector-executing","text":"<p>For mixed Cypher vector queries, <code>cypherglot</code> should parse the ordinary Cypher structure and carry vector intent forward as metadata or compiler-recognizable structure. A host runtime should then turn that into vector search plus a conditioned relational query path.</p>"},{"location":"guide/roadmap/","title":"Roadmap","text":"<p>The live roadmap is maintained in <code>things-to-do.md</code>.</p> <p>The high-level sequence is:</p> <ol> <li>establish the compiler-only repo boundary and API contract</li> <li>own parser generation and artifact verification in-repo</li> <li>build normalization and validation layers</li> <li>compile the admitted Cypher subset to SQLGlot AST</li> <li>broaden the admitted language carefully</li> <li>make the compiler vector-aware without making it vector-executing</li> <li>harden the package for public release</li> </ol>"}]}